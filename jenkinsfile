pipeline {
  agent any
  stages {
    stage("build") {
      steps {
        // sh "npm install"
        echo "build works"
      }
    }
    stage("test") {
      steps {
        sh "your_test_command_here"
      }
    }
    stage("deploy") {
      steps {
        sh "your_deploy_command_here"
      }
    }
  }
}
