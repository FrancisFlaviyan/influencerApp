pipeline {
  agent any
  stages {
    stage("build") {
      steps {
        // sh "npm install"
        echo "build works"
      }
    }
    stage("test") {
      steps {
        echo "test works"
      }
    }
    stage("deploy") {
      steps {
        echo "deploy works"
      }
    }
  }
}
